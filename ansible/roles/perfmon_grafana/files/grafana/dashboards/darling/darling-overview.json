{
  "uid": "darling-overview",
  "title": "Perfmon (Darling) - Overview",
  "tags": [
    "perfmon-darling"
  ],
  "timezone": "",
  "schemaVersion": 39,
  "editable": true,
  "graphTooltip": 1,
  "fiscalYearStartMonth": 0,
  "time": {
    "from": "now-3h",
    "to": "now"
  },
  "refresh": "1m",
  "weekStart": "",
  "annotations": {
    "list": []
  },
  "links": [
    {
      "title": "Fleet Overview",
      "icon": "dashboard",
      "type": "link",
      "url": "/d/darling-fleet?${__url_time_range}",
      "keepTime": true,
      "includeVars": false,
      "targetBlank": false
    },
    {
      "asDropdown": true,
      "icon": "external link",
      "includeVars": true,
      "keepTime": true,
      "tags": [
        "perfmon-darling"
      ],
      "targetBlank": false,
      "title": "All PerfMon (Darling) Dashboards",
      "type": "dashboards",
      "url": ""
    },
    {
      "asDropdown": true,
      "icon": "external link",
      "includeVars": true,
      "keepTime": true,
      "tags": [
        "finops-darling"
      ],
      "targetBlank": false,
      "title": "All FinOps (Darling) Dashboards",
      "type": "dashboards",
      "url": ""
    }
  ],
  "templating": {
    "list": [
      {
        "name": "server",
        "label": "Server",
        "type": "query",
        "datasource": {
          "type": "grafana-postgresql-datasource",
          "uid": "darling"
        },
        "query": "SELECT name AS __text, server_id AS __value FROM config.config_monitored_servers WHERE is_enabled ORDER BY name",
        "current": {},
        "options": [],
        "refresh": 1,
        "hide": 0,
        "multi": false,
        "includeAll": false,
        "sort": 1,
        "description": "Monitored SQL Server instance, from Darling's fleet registry."
      }
    ]
  },
  "panels": [
    {
      "id": 1,
      "type": "row",
      "title": "Overview",
      "collapsed": false,
      "gridPos": {
        "h": 1,
        "w": 24,
        "x": 0,
        "y": 0
      },
      "panels": []
    },
    {
      "id": 2,
      "type": "timeseries",
      "title": "CPU %",
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "darling"
      },
      "gridPos": {
        "h": 7,
        "w": 24,
        "x": 0,
        "y": 1
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "palette-classic"
          },
          "custom": {
            "drawStyle": "line",
            "lineInterpolation": "smooth",
            "lineWidth": 1,
            "fillOpacity": 12,
            "showPoints": "never",
            "spanNulls": true,
            "stacking": {
              "mode": "none",
              "group": "A"
            }
          },
          "unit": "percent",
          "max": 105,
          "min": 0
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "Baseline Lower"
            },
            "properties": [
              {
                "id": "custom.drawStyle",
                "value": "line"
              },
              {
                "id": "custom.lineWidth",
                "value": 0
              },
              {
                "id": "custom.hideFrom",
                "value": {
                  "legend": true,
                  "tooltip": true,
                  "viz": false
                }
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "Baseline Upper"
            },
            "properties": [
              {
                "id": "custom.fillBelowTo",
                "value": "Baseline Lower"
              },
              {
                "id": "custom.drawStyle",
                "value": "line"
              },
              {
                "id": "custom.lineWidth",
                "value": 0
              },
              {
                "id": "custom.hideFrom",
                "value": {
                  "legend": true,
                  "tooltip": true,
                  "viz": false
                }
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "Baseline Mean"
            },
            "properties": [
              {
                "id": "color",
                "value": {
                  "mode": "fixed",
                  "fixedColor": "#888888"
                }
              },
              {
                "id": "custom.lineStyle",
                "value": {
                  "fill": "dash",
                  "dash": [
                    10,
                    10
                  ]
                }
              },
              {
                "id": "custom.drawStyle",
                "value": "line"
              },
              {
                "id": "custom.lineWidth",
                "value": 1
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "Anomaly"
            },
            "properties": [
              {
                "id": "color",
                "value": {
                  "mode": "fixed",
                  "fixedColor": "#E57373"
                }
              },
              {
                "id": "custom.drawStyle",
                "value": "points"
              },
              {
                "id": "custom.pointSize",
                "value": 8
              }
            ]
          }
        ]
      },
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom",
          "showLegend": true,
          "calcs": []
        },
        "tooltip": {
          "mode": "multi",
          "sort": "desc"
        }
      },
      "targets": [
        {
          "refId": "A",
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "darling"
          },
          "editorMode": "code",
          "format": "time_series",
          "rawQuery": true,
          "rawSql": "\nWITH samples AS (\nSELECT\n    cpu.sample_time\n        - INTERVAL '15 minutes'\n          * ROUND(EXTRACT(EPOCH FROM (\n                MAX(cpu.sample_time) OVER (PARTITION BY cpu.server_id, cpu.collection_time)\n                - cpu.collection_time\n            )) / 900.0)::double precision AS time,\n    cpu.sqlserver_cpu_utilization AS sql_cpu,\n    cpu.sqlserver_cpu_utilization + COALESCE(cpu.other_process_cpu_utilization, 0) AS total_cpu\nFROM collect.v_cpu_utilization_stats AS cpu\nWHERE $__timeFilter(cpu.collection_time)\n  AND cpu.server_id IN (${server:csv})\n),\n\nref AS (\n    SELECT $__timeFrom()::timestamp AS ref_time\n),\nbuckets AS (\n    \nSELECT\n    EXTRACT(HOUR FROM collection_time)::int AS hour_of_day,\n    EXTRACT(DOW FROM collection_time)::int AS day_of_week,\n    SUM(cpu_sum)::double precision / NULLIF(SUM(cpu_count), 0) AS mean_val,\n    sqrt(GREATEST(\n        (SUM(cpu_sumsq) - POWER(SUM(cpu_sum), 2)::double precision / NULLIF(SUM(cpu_count), 0))\n        / NULLIF(SUM(cpu_count) - 1, 0), 0)) AS stddev_val,\n    SUM(cpu_count) AS sample_count,\n    COUNT(DISTINCT collection_time::date) AS distinct_days\nFROM collect.cpu_utilization_baseline, ref\nWHERE server_id IN (${server:csv})\nAND   collection_time >= ref.ref_time - INTERVAL '30 days'\nAND   collection_time <  ref.ref_time\nGROUP BY 1, 2\n\n),\ntarget AS (\n    SELECT EXTRACT(HOUR FROM ref_time)::int AS hour_of_day,\n           EXTRACT(DOW FROM ref_time)::int AS day_of_week\n    FROM ref\n),\nfull_bucket AS (\n    SELECT b.mean_val, b.stddev_val, b.sample_count\n    FROM buckets b, target t\n    WHERE b.hour_of_day = t.hour_of_day AND b.day_of_week = t.day_of_week\n),\nhour_mean AS (\n    SELECT\n        SUM(b.mean_val * b.sample_count) / NULLIF(SUM(b.sample_count), 0) AS grand_mean,\n        SUM(b.sample_count) AS sample_count\n    FROM buckets b, target t\n    WHERE b.hour_of_day = t.hour_of_day\n),\nhour_bucket AS (\n    SELECT\n        hm.grand_mean AS mean_val,\n        CASE WHEN hm.sample_count > 1 THEN sqrt(GREATEST(SUM(\n            (b.stddev_val * b.stddev_val) * GREATEST(b.sample_count - 1, 0)\n            + b.sample_count * (b.mean_val - hm.grand_mean) * (b.mean_val - hm.grand_mean)\n        ) / (hm.sample_count - 1), 0)) ELSE 0 END AS stddev_val,\n        hm.sample_count\n    FROM buckets b, target t, hour_mean hm\n    WHERE b.hour_of_day = t.hour_of_day\n    GROUP BY hm.grand_mean, hm.sample_count\n),\nflat_mean AS (\n    SELECT\n        SUM(b.mean_val * b.sample_count) / NULLIF(SUM(b.sample_count), 0) AS grand_mean,\n        SUM(b.sample_count) AS sample_count\n    FROM buckets b\n),\nflat_bucket AS (\n    SELECT\n        fm.grand_mean AS mean_val,\n        CASE WHEN fm.sample_count > 1 THEN sqrt(GREATEST(SUM(\n            (b.stddev_val * b.stddev_val) * GREATEST(b.sample_count - 1, 0)\n            + b.sample_count * (b.mean_val - fm.grand_mean) * (b.mean_val - fm.grand_mean)\n        ) / (fm.sample_count - 1), 0)) ELSE 0 END AS stddev_val,\n        fm.sample_count\n    FROM buckets b, flat_mean fm\n    GROUP BY fm.grand_mean, fm.sample_count\n),\nselected AS (\n    SELECT\n        CASE\n            WHEN (SELECT sample_count FROM full_bucket) >= 10 THEN (SELECT mean_val FROM full_bucket)\n            WHEN (SELECT sample_count FROM hour_bucket) >= 10 THEN (SELECT mean_val FROM hour_bucket)\n            WHEN (SELECT sample_count FROM flat_bucket) >= 3 THEN (SELECT mean_val FROM flat_bucket)\n        END AS mean_val,\n        CASE\n            WHEN (SELECT sample_count FROM full_bucket) >= 10 THEN (SELECT stddev_val FROM full_bucket)\n            WHEN (SELECT sample_count FROM hour_bucket) >= 10 THEN (SELECT stddev_val FROM hour_bucket)\n            WHEN (SELECT sample_count FROM flat_bucket) >= 3 THEN (SELECT stddev_val FROM flat_bucket)\n        END AS stddev_val,\n        CASE\n            WHEN (SELECT sample_count FROM full_bucket) >= 10 THEN (SELECT sample_count FROM full_bucket)\n            WHEN (SELECT sample_count FROM hour_bucket) >= 10 THEN (SELECT sample_count FROM hour_bucket)\n            WHEN (SELECT sample_count FROM flat_bucket) >= 3 THEN (SELECT sample_count FROM flat_bucket)\n        END AS sample_count\n),\nbaseline AS (\n    SELECT\n        mean_val,\n        CASE\n            WHEN sample_count IS NULL THEN NULL\n            WHEN mean_val = 0 AND COALESCE(stddev_val, 0) <= 0 THEN 0\n            ELSE GREATEST(stddev_val, mean_val * 0.01, 5.0)\n        END AS effective_stddev,\n        sample_count\n    FROM selected\n),\nbounds AS (\n    SELECT\n        mean_val,\n        mean_val + 2 * effective_stddev AS upper_val,\n        GREATEST(0, mean_val - 2 * effective_stddev) AS lower_val\n    FROM baseline\n    WHERE sample_count > 0 AND effective_stddev > 0\n)\nSELECT time, 'SQL CPU' AS metric, sql_cpu AS value FROM samples\nUNION ALL\nSELECT time, 'Total CPU', total_cpu FROM samples\n\nUNION ALL\nSELECT $__timeFrom(), 'Baseline Lower', lower_val FROM bounds\nUNION ALL\nSELECT $__timeTo(), 'Baseline Lower', lower_val FROM bounds\nUNION ALL\nSELECT $__timeFrom(), 'Baseline Upper', upper_val FROM bounds\nUNION ALL\nSELECT $__timeTo(), 'Baseline Upper', upper_val FROM bounds\nUNION ALL\nSELECT $__timeFrom(), 'Baseline Mean', mean_val FROM bounds\nUNION ALL\nSELECT $__timeTo(), 'Baseline Mean', mean_val FROM bounds\nUNION ALL\nSELECT a.time, 'Anomaly', a.value\nFROM (SELECT time, sql_cpu AS value FROM samples) AS a, bounds b\nWHERE (a.value > b.upper_val AND a.value >= 10) OR a.value < b.lower_val\nORDER BY 1\n"
        }
      ]
    },
    {
      "id": 3,
      "type": "timeseries",
      "title": "Wait ms/sec",
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "darling"
      },
      "gridPos": {
        "h": 7,
        "w": 24,
        "x": 0,
        "y": 8
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "palette-classic"
          },
          "custom": {
            "drawStyle": "line",
            "lineInterpolation": "smooth",
            "lineWidth": 1,
            "fillOpacity": 12,
            "showPoints": "never",
            "spanNulls": true,
            "stacking": {
              "mode": "none",
              "group": "A"
            },
            "axisLabel": "ms/sec"
          },
          "unit": "short"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "Baseline Lower"
            },
            "properties": [
              {
                "id": "custom.drawStyle",
                "value": "line"
              },
              {
                "id": "custom.lineWidth",
                "value": 0
              },
              {
                "id": "custom.hideFrom",
                "value": {
                  "legend": true,
                  "tooltip": true,
                  "viz": false
                }
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "Baseline Upper"
            },
            "properties": [
              {
                "id": "custom.fillBelowTo",
                "value": "Baseline Lower"
              },
              {
                "id": "custom.drawStyle",
                "value": "line"
              },
              {
                "id": "custom.lineWidth",
                "value": 0
              },
              {
                "id": "custom.hideFrom",
                "value": {
                  "legend": true,
                  "tooltip": true,
                  "viz": false
                }
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "Baseline Mean"
            },
            "properties": [
              {
                "id": "color",
                "value": {
                  "mode": "fixed",
                  "fixedColor": "#888888"
                }
              },
              {
                "id": "custom.lineStyle",
                "value": {
                  "fill": "dash",
                  "dash": [
                    10,
                    10
                  ]
                }
              },
              {
                "id": "custom.drawStyle",
                "value": "line"
              },
              {
                "id": "custom.lineWidth",
                "value": 1
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "Anomaly"
            },
            "properties": [
              {
                "id": "color",
                "value": {
                  "mode": "fixed",
                  "fixedColor": "#E57373"
                }
              },
              {
                "id": "custom.drawStyle",
                "value": "points"
              },
              {
                "id": "custom.pointSize",
                "value": 8
              }
            ]
          }
        ]
      },
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom",
          "showLegend": true,
          "calcs": []
        },
        "tooltip": {
          "mode": "multi",
          "sort": "desc"
        }
      },
      "targets": [
        {
          "refId": "A",
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "darling"
          },
          "editorMode": "code",
          "format": "time_series",
          "rawQuery": true,
          "rawSql": "\nWITH samples AS (\nWITH per_collection AS (\n    SELECT\n        collection_time,\n        SUM(delta_wait_time_ms) AS total_delta_ms,\n        EXTRACT(EPOCH FROM (date_trunc('second', collection_time)\n            - date_trunc('second', LAG(collection_time) OVER (ORDER BY collection_time)))) AS interval_seconds\n    FROM collect.v_wait_stats\n    WHERE $__timeFilter(collection_time)\n      AND server_id IN (${server:csv})\n    GROUP BY collection_time\n)\nSELECT\n    collection_time AS time,\n    CASE WHEN interval_seconds > 0 THEN total_delta_ms::double precision / interval_seconds ELSE 0 END AS value\nFROM per_collection\n),\n\nref AS (\n    SELECT $__timeFrom()::timestamp AS ref_time\n),\nbuckets AS (\n    \nWITH per_collection AS (\n    SELECT\n        collection_time,\n        total_wait_ms::double precision AS total_wait_ms,\n        EXTRACT(EPOCH FROM (date_trunc('second', collection_time)\n            - date_trunc('second', LAG(collection_time) OVER (ORDER BY collection_time)))) AS interval_sec\n    FROM collect.wait_stats_baseline, ref\n    WHERE server_id IN (${server:csv})\n    AND   collection_time >= ref.ref_time - INTERVAL '30 days'\n    AND   collection_time <  ref.ref_time\n),\nwith_rate AS (\n    SELECT collection_time,\n           CASE WHEN interval_sec > 0 THEN total_wait_ms / interval_sec ELSE 0 END AS ms_per_sec\n    FROM per_collection\n    WHERE interval_sec IS NOT NULL\n),\nwith_lag AS (\n    SELECT collection_time, ms_per_sec,\n           COALESCE(LAG(ms_per_sec) OVER (ORDER BY collection_time), 0) AS prior_ms_per_sec\n    FROM with_rate\n)\nSELECT\n    EXTRACT(HOUR FROM collection_time)::int AS hour_of_day,\n    EXTRACT(DOW FROM collection_time)::int AS day_of_week,\n    AVG(ms_per_sec) AS mean_val,\n    COALESCE(STDDEV_SAMP(ms_per_sec), 0) AS stddev_val,\n    COUNT(*) AS sample_count,\n    COUNT(DISTINCT collection_time::date) AS distinct_days\nFROM with_lag\nWHERE NOT (ms_per_sec = 0 AND prior_ms_per_sec > 100)\nGROUP BY 1, 2\n\n),\ntarget AS (\n    SELECT EXTRACT(HOUR FROM ref_time)::int AS hour_of_day,\n           EXTRACT(DOW FROM ref_time)::int AS day_of_week\n    FROM ref\n),\nfull_bucket AS (\n    SELECT b.mean_val, b.stddev_val, b.sample_count\n    FROM buckets b, target t\n    WHERE b.hour_of_day = t.hour_of_day AND b.day_of_week = t.day_of_week\n),\nhour_mean AS (\n    SELECT\n        SUM(b.mean_val * b.sample_count) / NULLIF(SUM(b.sample_count), 0) AS grand_mean,\n        SUM(b.sample_count) AS sample_count\n    FROM buckets b, target t\n    WHERE b.hour_of_day = t.hour_of_day\n),\nhour_bucket AS (\n    SELECT\n        hm.grand_mean AS mean_val,\n        CASE WHEN hm.sample_count > 1 THEN sqrt(GREATEST(SUM(\n            (b.stddev_val * b.stddev_val) * GREATEST(b.sample_count - 1, 0)\n            + b.sample_count * (b.mean_val - hm.grand_mean) * (b.mean_val - hm.grand_mean)\n        ) / (hm.sample_count - 1), 0)) ELSE 0 END AS stddev_val,\n        hm.sample_count\n    FROM buckets b, target t, hour_mean hm\n    WHERE b.hour_of_day = t.hour_of_day\n    GROUP BY hm.grand_mean, hm.sample_count\n),\nflat_mean AS (\n    SELECT\n        SUM(b.mean_val * b.sample_count) / NULLIF(SUM(b.sample_count), 0) AS grand_mean,\n        SUM(b.sample_count) AS sample_count\n    FROM buckets b\n),\nflat_bucket AS (\n    SELECT\n        fm.grand_mean AS mean_val,\n        CASE WHEN fm.sample_count > 1 THEN sqrt(GREATEST(SUM(\n            (b.stddev_val * b.stddev_val) * GREATEST(b.sample_count - 1, 0)\n            + b.sample_count * (b.mean_val - fm.grand_mean) * (b.mean_val - fm.grand_mean)\n        ) / (fm.sample_count - 1), 0)) ELSE 0 END AS stddev_val,\n        fm.sample_count\n    FROM buckets b, flat_mean fm\n    GROUP BY fm.grand_mean, fm.sample_count\n),\nselected AS (\n    SELECT\n        CASE\n            WHEN (SELECT sample_count FROM full_bucket) >= 10 THEN (SELECT mean_val FROM full_bucket)\n            WHEN (SELECT sample_count FROM hour_bucket) >= 10 THEN (SELECT mean_val FROM hour_bucket)\n            WHEN (SELECT sample_count FROM flat_bucket) >= 3 THEN (SELECT mean_val FROM flat_bucket)\n        END AS mean_val,\n        CASE\n            WHEN (SELECT sample_count FROM full_bucket) >= 10 THEN (SELECT stddev_val FROM full_bucket)\n            WHEN (SELECT sample_count FROM hour_bucket) >= 10 THEN (SELECT stddev_val FROM hour_bucket)\n            WHEN (SELECT sample_count FROM flat_bucket) >= 3 THEN (SELECT stddev_val FROM flat_bucket)\n        END AS stddev_val,\n        CASE\n            WHEN (SELECT sample_count FROM full_bucket) >= 10 THEN (SELECT sample_count FROM full_bucket)\n            WHEN (SELECT sample_count FROM hour_bucket) >= 10 THEN (SELECT sample_count FROM hour_bucket)\n            WHEN (SELECT sample_count FROM flat_bucket) >= 3 THEN (SELECT sample_count FROM flat_bucket)\n        END AS sample_count\n),\nbaseline AS (\n    SELECT\n        mean_val,\n        CASE\n            WHEN sample_count IS NULL THEN NULL\n            WHEN mean_val = 0 AND COALESCE(stddev_val, 0) <= 0 THEN 0\n            ELSE GREATEST(stddev_val, mean_val * 0.01, 0.0)\n        END AS effective_stddev,\n        sample_count\n    FROM selected\n),\nbounds AS (\n    SELECT\n        mean_val,\n        mean_val + 2 * effective_stddev AS upper_val,\n        GREATEST(0, mean_val - 2 * effective_stddev) AS lower_val\n    FROM baseline\n    WHERE sample_count > 0 AND effective_stddev > 0\n)\nSELECT time, 'Wait ms/sec' AS metric, value FROM samples\n\nUNION ALL\nSELECT $__timeFrom(), 'Baseline Lower', lower_val FROM bounds\nUNION ALL\nSELECT $__timeTo(), 'Baseline Lower', lower_val FROM bounds\nUNION ALL\nSELECT $__timeFrom(), 'Baseline Upper', upper_val FROM bounds\nUNION ALL\nSELECT $__timeTo(), 'Baseline Upper', upper_val FROM bounds\nUNION ALL\nSELECT $__timeFrom(), 'Baseline Mean', mean_val FROM bounds\nUNION ALL\nSELECT $__timeTo(), 'Baseline Mean', mean_val FROM bounds\nUNION ALL\nSELECT a.time, 'Anomaly', a.value\nFROM (SELECT time, value FROM samples) AS a, bounds b\nWHERE (a.value > b.upper_val AND a.value >= 100) OR a.value < b.lower_val\nORDER BY 1\n"
        }
      ]
    },
    {
      "id": 4,
      "type": "timeseries",
      "title": "Blocking & Deadlocking",
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "darling"
      },
      "gridPos": {
        "h": 7,
        "w": 24,
        "x": 0,
        "y": 15
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "palette-classic"
          },
          "custom": {
            "drawStyle": "bars",
            "lineInterpolation": "smooth",
            "lineWidth": 1,
            "fillOpacity": 80,
            "showPoints": "never",
            "spanNulls": true,
            "stacking": {
              "mode": "none",
              "group": "A"
            },
            "axisLabel": "events"
          },
          "unit": "short"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "Baseline Lower"
            },
            "properties": [
              {
                "id": "custom.drawStyle",
                "value": "line"
              },
              {
                "id": "custom.lineWidth",
                "value": 0
              },
              {
                "id": "custom.hideFrom",
                "value": {
                  "legend": true,
                  "tooltip": true,
                  "viz": false
                }
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "Baseline Upper"
            },
            "properties": [
              {
                "id": "custom.fillBelowTo",
                "value": "Baseline Lower"
              },
              {
                "id": "custom.drawStyle",
                "value": "line"
              },
              {
                "id": "custom.lineWidth",
                "value": 0
              },
              {
                "id": "custom.hideFrom",
                "value": {
                  "legend": true,
                  "tooltip": true,
                  "viz": false
                }
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "Baseline Mean"
            },
            "properties": [
              {
                "id": "color",
                "value": {
                  "mode": "fixed",
                  "fixedColor": "#888888"
                }
              },
              {
                "id": "custom.lineStyle",
                "value": {
                  "fill": "dash",
                  "dash": [
                    10,
                    10
                  ]
                }
              },
              {
                "id": "custom.drawStyle",
                "value": "line"
              },
              {
                "id": "custom.lineWidth",
                "value": 1
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "Anomaly"
            },
            "properties": [
              {
                "id": "color",
                "value": {
                  "mode": "fixed",
                  "fixedColor": "#E57373"
                }
              },
              {
                "id": "custom.drawStyle",
                "value": "points"
              },
              {
                "id": "custom.pointSize",
                "value": 8
              }
            ]
          }
        ]
      },
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom",
          "showLegend": true,
          "calcs": []
        },
        "tooltip": {
          "mode": "multi",
          "sort": "desc"
        }
      },
      "targets": [
        {
          "refId": "A",
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "darling"
          },
          "editorMode": "code",
          "format": "time_series",
          "rawQuery": true,
          "rawSql": "\nWITH blocking_data AS (\nWITH bpr AS (\n    SELECT date_trunc('minute', event_time) AS t, COUNT(*) AS c\n    FROM collect.v_blocked_process_reports\n    WHERE $__timeFilter(collection_time)\n      AND server_id IN (${server:csv})\n    GROUP BY 1\n),\ndmv AS (\n    SELECT date_trunc('minute', event_time) AS t, COUNT(*) AS c\n    FROM collect.v_dmv_blocking_snapshots\n    WHERE $__timeFilter(collection_time)\n      AND server_id IN (${server:csv})\n      AND NOT EXISTS (SELECT 1 FROM bpr)\n    GROUP BY 1\n)\nSELECT t AS time, c AS value FROM bpr\nUNION ALL\nSELECT t, c FROM dmv\n),\ndeadlock_data AS (\nSELECT date_trunc('minute', deadlock_time) AS time, COUNT(*) AS value\nFROM collect.v_deadlocks\nWHERE $__timeFilter(collection_time)\n  AND server_id IN (${server:csv})\nGROUP BY 1\n),\n\nref AS (\n    SELECT $__timeFrom()::timestamp AS ref_time\n),\nbuckets AS (\n    \nWITH per_minute AS (\n    SELECT DATE_TRUNC('minute', collection_time) AS minute_bucket,\n           SUM(event_count)::double precision AS event_count\n    FROM collect.blocked_process_baseline, ref\n    WHERE server_id IN (${server:csv})\n    AND   collection_time >= ref.ref_time - INTERVAL '30 days'\n    AND   collection_time <  ref.ref_time\n    GROUP BY minute_bucket\n)\nSELECT\n    EXTRACT(HOUR FROM minute_bucket)::int AS hour_of_day,\n    EXTRACT(DOW FROM minute_bucket)::int AS day_of_week,\n    AVG(event_count) AS mean_val,\n    COALESCE(STDDEV_SAMP(event_count), 0) AS stddev_val,\n    COUNT(*) AS sample_count,\n    COUNT(DISTINCT minute_bucket::date) AS distinct_days\nFROM per_minute\nGROUP BY 1, 2\n\n),\ntarget AS (\n    SELECT EXTRACT(HOUR FROM ref_time)::int AS hour_of_day,\n           EXTRACT(DOW FROM ref_time)::int AS day_of_week\n    FROM ref\n),\nfull_bucket AS (\n    SELECT b.mean_val, b.stddev_val, b.sample_count\n    FROM buckets b, target t\n    WHERE b.hour_of_day = t.hour_of_day AND b.day_of_week = t.day_of_week\n),\nhour_mean AS (\n    SELECT\n        SUM(b.mean_val * b.sample_count) / NULLIF(SUM(b.sample_count), 0) AS grand_mean,\n        SUM(b.sample_count) AS sample_count\n    FROM buckets b, target t\n    WHERE b.hour_of_day = t.hour_of_day\n),\nhour_bucket AS (\n    SELECT\n        hm.grand_mean AS mean_val,\n        CASE WHEN hm.sample_count > 1 THEN sqrt(GREATEST(SUM(\n            (b.stddev_val * b.stddev_val) * GREATEST(b.sample_count - 1, 0)\n            + b.sample_count * (b.mean_val - hm.grand_mean) * (b.mean_val - hm.grand_mean)\n        ) / (hm.sample_count - 1), 0)) ELSE 0 END AS stddev_val,\n        hm.sample_count\n    FROM buckets b, target t, hour_mean hm\n    WHERE b.hour_of_day = t.hour_of_day\n    GROUP BY hm.grand_mean, hm.sample_count\n),\nflat_mean AS (\n    SELECT\n        SUM(b.mean_val * b.sample_count) / NULLIF(SUM(b.sample_count), 0) AS grand_mean,\n        SUM(b.sample_count) AS sample_count\n    FROM buckets b\n),\nflat_bucket AS (\n    SELECT\n        fm.grand_mean AS mean_val,\n        CASE WHEN fm.sample_count > 1 THEN sqrt(GREATEST(SUM(\n            (b.stddev_val * b.stddev_val) * GREATEST(b.sample_count - 1, 0)\n            + b.sample_count * (b.mean_val - fm.grand_mean) * (b.mean_val - fm.grand_mean)\n        ) / (fm.sample_count - 1), 0)) ELSE 0 END AS stddev_val,\n        fm.sample_count\n    FROM buckets b, flat_mean fm\n    GROUP BY fm.grand_mean, fm.sample_count\n),\nselected AS (\n    SELECT\n        CASE\n            WHEN (SELECT sample_count FROM full_bucket) >= 10 THEN (SELECT mean_val FROM full_bucket)\n            WHEN (SELECT sample_count FROM hour_bucket) >= 10 THEN (SELECT mean_val FROM hour_bucket)\n            WHEN (SELECT sample_count FROM flat_bucket) >= 3 THEN (SELECT mean_val FROM flat_bucket)\n        END AS mean_val,\n        CASE\n            WHEN (SELECT sample_count FROM full_bucket) >= 10 THEN (SELECT stddev_val FROM full_bucket)\n            WHEN (SELECT sample_count FROM hour_bucket) >= 10 THEN (SELECT stddev_val FROM hour_bucket)\n            WHEN (SELECT sample_count FROM flat_bucket) >= 3 THEN (SELECT stddev_val FROM flat_bucket)\n        END AS stddev_val,\n        CASE\n            WHEN (SELECT sample_count FROM full_bucket) >= 10 THEN (SELECT sample_count FROM full_bucket)\n            WHEN (SELECT sample_count FROM hour_bucket) >= 10 THEN (SELECT sample_count FROM hour_bucket)\n            WHEN (SELECT sample_count FROM flat_bucket) >= 3 THEN (SELECT sample_count FROM flat_bucket)\n        END AS sample_count\n),\nbaseline AS (\n    SELECT\n        mean_val,\n        CASE\n            WHEN sample_count IS NULL THEN NULL\n            WHEN mean_val = 0 AND COALESCE(stddev_val, 0) <= 0 THEN 0\n            ELSE GREATEST(stddev_val, mean_val * 0.01, 0.0)\n        END AS effective_stddev,\n        sample_count\n    FROM selected\n),\nbounds AS (\n    SELECT\n        mean_val,\n        mean_val + 2 * effective_stddev AS upper_val,\n        GREATEST(0, mean_val - 2 * effective_stddev) AS lower_val\n    FROM baseline\n    WHERE sample_count > 0 AND effective_stddev > 0\n)\nSELECT time, 'Blocking' AS metric, value FROM blocking_data\nUNION ALL\nSELECT time, 'Deadlocks', value FROM deadlock_data\n\nUNION ALL\nSELECT $__timeFrom(), 'Baseline Lower', lower_val FROM bounds\nUNION ALL\nSELECT $__timeTo(), 'Baseline Lower', lower_val FROM bounds\nUNION ALL\nSELECT $__timeFrom(), 'Baseline Upper', upper_val FROM bounds\nUNION ALL\nSELECT $__timeTo(), 'Baseline Upper', upper_val FROM bounds\nUNION ALL\nSELECT $__timeFrom(), 'Baseline Mean', mean_val FROM bounds\nUNION ALL\nSELECT $__timeTo(), 'Baseline Mean', mean_val FROM bounds\nORDER BY 1\n"
        }
      ]
    },
    {
      "id": 5,
      "type": "timeseries",
      "title": "Buffer Pool MB",
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "darling"
      },
      "gridPos": {
        "h": 7,
        "w": 24,
        "x": 0,
        "y": 22
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "palette-classic"
          },
          "custom": {
            "drawStyle": "line",
            "lineInterpolation": "smooth",
            "lineWidth": 1,
            "fillOpacity": 12,
            "showPoints": "never",
            "spanNulls": true,
            "stacking": {
              "mode": "none",
              "group": "A"
            }
          },
          "unit": "decmbytes"
        },
        "overrides": []
      },
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom",
          "showLegend": true,
          "calcs": []
        },
        "tooltip": {
          "mode": "multi",
          "sort": "desc"
        }
      },
      "targets": [
        {
          "refId": "A",
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "darling"
          },
          "editorMode": "code",
          "format": "time_series",
          "rawQuery": true,
          "rawSql": "\nSELECT\n    collection_time AS time,\n    'Buffer Pool MB' AS metric,\n    CAST(buffer_pool_mb AS double precision) AS value\nFROM collect.v_memory_stats\nWHERE $__timeFilter(collection_time)\n  AND server_id IN (${server:csv})\nORDER BY 1\n"
        }
      ]
    },
    {
      "id": 6,
      "type": "timeseries",
      "title": "I/O Latency",
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "darling"
      },
      "gridPos": {
        "h": 7,
        "w": 24,
        "x": 0,
        "y": 29
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "palette-classic"
          },
          "custom": {
            "drawStyle": "line",
            "lineInterpolation": "smooth",
            "lineWidth": 1,
            "fillOpacity": 12,
            "showPoints": "never",
            "spanNulls": true,
            "stacking": {
              "mode": "none",
              "group": "A"
            },
            "axisLabel": "ms"
          },
          "unit": "ms"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "Baseline Lower"
            },
            "properties": [
              {
                "id": "custom.drawStyle",
                "value": "line"
              },
              {
                "id": "custom.lineWidth",
                "value": 0
              },
              {
                "id": "custom.hideFrom",
                "value": {
                  "legend": true,
                  "tooltip": true,
                  "viz": false
                }
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "Baseline Upper"
            },
            "properties": [
              {
                "id": "custom.fillBelowTo",
                "value": "Baseline Lower"
              },
              {
                "id": "custom.drawStyle",
                "value": "line"
              },
              {
                "id": "custom.lineWidth",
                "value": 0
              },
              {
                "id": "custom.hideFrom",
                "value": {
                  "legend": true,
                  "tooltip": true,
                  "viz": false
                }
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "Baseline Mean"
            },
            "properties": [
              {
                "id": "color",
                "value": {
                  "mode": "fixed",
                  "fixedColor": "#888888"
                }
              },
              {
                "id": "custom.lineStyle",
                "value": {
                  "fill": "dash",
                  "dash": [
                    10,
                    10
                  ]
                }
              },
              {
                "id": "custom.drawStyle",
                "value": "line"
              },
              {
                "id": "custom.lineWidth",
                "value": 1
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "Anomaly"
            },
            "properties": [
              {
                "id": "color",
                "value": {
                  "mode": "fixed",
                  "fixedColor": "#E57373"
                }
              },
              {
                "id": "custom.drawStyle",
                "value": "points"
              },
              {
                "id": "custom.pointSize",
                "value": 8
              }
            ]
          }
        ]
      },
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom",
          "showLegend": true,
          "calcs": []
        },
        "tooltip": {
          "mode": "multi",
          "sort": "desc"
        }
      },
      "targets": [
        {
          "refId": "A",
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "darling"
          },
          "editorMode": "code",
          "format": "time_series",
          "rawQuery": true,
          "rawSql": "\nWITH samples AS (\nWITH top_files AS (\n    SELECT database_name, file_name\n    FROM collect.v_file_io_stats\n    WHERE $__timeFilter(collection_time)\n      AND server_id IN (${server:csv})\n      AND (delta_reads > 0 OR delta_writes > 0)\n    GROUP BY database_name, file_name\n    ORDER BY SUM(delta_reads + delta_writes) DESC\n    LIMIT 10\n),\nper_file AS (\n    SELECT\n        f.collection_time,\n        CASE WHEN SUM(f.delta_reads) > 0\n             THEN SUM(f.delta_stall_read_ms::double precision) / SUM(f.delta_reads)\n             ELSE 0 END AS avg_read_latency_ms\n    FROM collect.v_file_io_stats AS f\n    JOIN top_files tf ON tf.database_name = f.database_name AND tf.file_name = f.file_name\n    WHERE $__timeFilter(f.collection_time)\n      AND f.server_id IN (${server:csv})\n    GROUP BY f.collection_time, f.database_name, f.file_name\n)\nSELECT collection_time AS time, AVG(avg_read_latency_ms) AS value\nFROM per_file\nGROUP BY collection_time\n),\n\nref AS (\n    SELECT $__timeFrom()::timestamp AS ref_time\n),\nbuckets AS (\n    \nSELECT\n    EXTRACT(HOUR FROM collection_time)::int AS hour_of_day,\n    EXTRACT(DOW FROM collection_time)::int AS day_of_week,\n    SUM(ratio_sum) / NULLIF(SUM(ratio_count), 0) AS mean_val,\n    sqrt(GREATEST(\n        (SUM(ratio_sumsq) - POWER(SUM(ratio_sum), 2) / NULLIF(SUM(ratio_count), 0))\n        / NULLIF(SUM(ratio_count) - 1, 0), 0)) AS stddev_val,\n    SUM(row_count) AS sample_count,\n    COUNT(DISTINCT collection_time::date) AS distinct_days\nFROM collect.file_io_baseline, ref\nWHERE server_id IN (${server:csv})\nAND   collection_time >= ref.ref_time - INTERVAL '30 days'\nAND   collection_time <  ref.ref_time\nGROUP BY 1, 2\n\n),\ntarget AS (\n    SELECT EXTRACT(HOUR FROM ref_time)::int AS hour_of_day,\n           EXTRACT(DOW FROM ref_time)::int AS day_of_week\n    FROM ref\n),\nfull_bucket AS (\n    SELECT b.mean_val, b.stddev_val, b.sample_count\n    FROM buckets b, target t\n    WHERE b.hour_of_day = t.hour_of_day AND b.day_of_week = t.day_of_week\n),\nhour_mean AS (\n    SELECT\n        SUM(b.mean_val * b.sample_count) / NULLIF(SUM(b.sample_count), 0) AS grand_mean,\n        SUM(b.sample_count) AS sample_count\n    FROM buckets b, target t\n    WHERE b.hour_of_day = t.hour_of_day\n),\nhour_bucket AS (\n    SELECT\n        hm.grand_mean AS mean_val,\n        CASE WHEN hm.sample_count > 1 THEN sqrt(GREATEST(SUM(\n            (b.stddev_val * b.stddev_val) * GREATEST(b.sample_count - 1, 0)\n            + b.sample_count * (b.mean_val - hm.grand_mean) * (b.mean_val - hm.grand_mean)\n        ) / (hm.sample_count - 1), 0)) ELSE 0 END AS stddev_val,\n        hm.sample_count\n    FROM buckets b, target t, hour_mean hm\n    WHERE b.hour_of_day = t.hour_of_day\n    GROUP BY hm.grand_mean, hm.sample_count\n),\nflat_mean AS (\n    SELECT\n        SUM(b.mean_val * b.sample_count) / NULLIF(SUM(b.sample_count), 0) AS grand_mean,\n        SUM(b.sample_count) AS sample_count\n    FROM buckets b\n),\nflat_bucket AS (\n    SELECT\n        fm.grand_mean AS mean_val,\n        CASE WHEN fm.sample_count > 1 THEN sqrt(GREATEST(SUM(\n            (b.stddev_val * b.stddev_val) * GREATEST(b.sample_count - 1, 0)\n            + b.sample_count * (b.mean_val - fm.grand_mean) * (b.mean_val - fm.grand_mean)\n        ) / (fm.sample_count - 1), 0)) ELSE 0 END AS stddev_val,\n        fm.sample_count\n    FROM buckets b, flat_mean fm\n    GROUP BY fm.grand_mean, fm.sample_count\n),\nselected AS (\n    SELECT\n        CASE\n            WHEN (SELECT sample_count FROM full_bucket) >= 10 THEN (SELECT mean_val FROM full_bucket)\n            WHEN (SELECT sample_count FROM hour_bucket) >= 10 THEN (SELECT mean_val FROM hour_bucket)\n            WHEN (SELECT sample_count FROM flat_bucket) >= 3 THEN (SELECT mean_val FROM flat_bucket)\n        END AS mean_val,\n        CASE\n            WHEN (SELECT sample_count FROM full_bucket) >= 10 THEN (SELECT stddev_val FROM full_bucket)\n            WHEN (SELECT sample_count FROM hour_bucket) >= 10 THEN (SELECT stddev_val FROM hour_bucket)\n            WHEN (SELECT sample_count FROM flat_bucket) >= 3 THEN (SELECT stddev_val FROM flat_bucket)\n        END AS stddev_val,\n        CASE\n            WHEN (SELECT sample_count FROM full_bucket) >= 10 THEN (SELECT sample_count FROM full_bucket)\n            WHEN (SELECT sample_count FROM hour_bucket) >= 10 THEN (SELECT sample_count FROM hour_bucket)\n            WHEN (SELECT sample_count FROM flat_bucket) >= 3 THEN (SELECT sample_count FROM flat_bucket)\n        END AS sample_count\n),\nbaseline AS (\n    SELECT\n        mean_val,\n        CASE\n            WHEN sample_count IS NULL THEN NULL\n            WHEN mean_val = 0 AND COALESCE(stddev_val, 0) <= 0 THEN 0\n            ELSE GREATEST(stddev_val, mean_val * 0.01, 2.5)\n        END AS effective_stddev,\n        sample_count\n    FROM selected\n),\nbounds AS (\n    SELECT\n        mean_val,\n        mean_val + 2 * effective_stddev AS upper_val,\n        GREATEST(0, mean_val - 2 * effective_stddev) AS lower_val\n    FROM baseline\n    WHERE sample_count > 0 AND effective_stddev > 0\n)\nSELECT time, 'I/O ms' AS metric, value FROM samples\n\nUNION ALL\nSELECT $__timeFrom(), 'Baseline Lower', lower_val FROM bounds\nUNION ALL\nSELECT $__timeTo(), 'Baseline Lower', lower_val FROM bounds\nUNION ALL\nSELECT $__timeFrom(), 'Baseline Upper', upper_val FROM bounds\nUNION ALL\nSELECT $__timeTo(), 'Baseline Upper', upper_val FROM bounds\nUNION ALL\nSELECT $__timeFrom(), 'Baseline Mean', mean_val FROM bounds\nUNION ALL\nSELECT $__timeTo(), 'Baseline Mean', mean_val FROM bounds\nUNION ALL\nSELECT a.time, 'Anomaly', a.value\nFROM (SELECT time, value FROM samples) AS a, bounds b\nWHERE (a.value > b.upper_val AND a.value >= 2) OR a.value < b.lower_val\nORDER BY 1\n"
        }
      ]
    }
  ]
}
