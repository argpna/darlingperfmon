{
  "uid": "darling-finops-database-resources",
  "title": "FinOps - Database Resources",
  "tags": [
    "finops"
  ],
  "timezone": "",
  "schemaVersion": 39,
  "editable": true,
  "graphTooltip": 1,
  "fiscalYearStartMonth": 0,
  "time": {
    "from": "now-24h",
    "to": "now"
  },
  "refresh": "5m",
  "weekStart": "",
  "annotations": {
    "list": []
  },
  "links": [
    {
      "title": "Fleet Overview",
      "icon": "dashboard",
      "type": "link",
      "url": "/d/darling-fleet?${__url_time_range}",
      "keepTime": true,
      "includeVars": false,
      "targetBlank": false
    },
    {
      "asDropdown": true,
      "icon": "external link",
      "includeVars": true,
      "keepTime": true,
      "tags": [
        "finops"
      ],
      "targetBlank": false,
      "title": "All FinOps Dashboards",
      "type": "dashboards",
      "url": ""
    },
    {
      "asDropdown": true,
      "icon": "external link",
      "includeVars": true,
      "keepTime": true,
      "tags": [
        "perfmon"
      ],
      "targetBlank": false,
      "title": "All PerfMon Dashboards",
      "type": "dashboards",
      "url": ""
    }
  ],
  "templating": {
    "list": [
      {
        "name": "server",
        "label": "Server",
        "type": "query",
        "datasource": {
          "type": "grafana-postgresql-datasource",
          "uid": "darling"
        },
        "query": "SELECT name AS __text, server_id AS __value FROM config.config_monitored_servers WHERE is_enabled ORDER BY name",
        "current": {},
        "options": [],
        "refresh": 1,
        "hide": 0,
        "multi": true,
        "includeAll": true,
        "sort": 1,
        "description": "Monitored SQL Server instance, from Darling's fleet registry."
      }
    ]
  },
  "panels": [
    {
      "id": 1,
      "type": "table",
      "title": "Database Resource Usage",
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "darling"
      },
      "gridPos": {
        "h": 20,
        "w": 24,
        "x": 0,
        "y": 0
      },
      "fieldConfig": {
        "defaults": {
          "custom": {
            "align": "auto",
            "cellOptions": {
              "type": "auto"
            },
            "filterable": true
          },
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "green",
                "value": null
              }
            ]
          }
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "CPU Time (ms)"
            },
            "properties": [
              {
                "id": "unit",
                "value": "ms"
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "CPU %"
            },
            "properties": [
              {
                "id": "min",
                "value": 0
              },
              {
                "id": "max",
                "value": 100
              },
              {
                "id": "unit",
                "value": "percent"
              },
              {
                "id": "color",
                "value": {
                  "mode": "fixed",
                  "fixedColor": "blue"
                }
              },
              {
                "id": "custom.cellOptions",
                "value": {
                  "type": "gauge",
                  "mode": "basic"
                }
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "IO %"
            },
            "properties": [
              {
                "id": "min",
                "value": 0
              },
              {
                "id": "max",
                "value": 100
              },
              {
                "id": "unit",
                "value": "percent"
              },
              {
                "id": "color",
                "value": {
                  "mode": "fixed",
                  "fixedColor": "blue"
                }
              },
              {
                "id": "custom.cellOptions",
                "value": {
                  "type": "gauge",
                  "mode": "basic"
                }
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "IO Read MB"
            },
            "properties": [
              {
                "id": "unit",
                "value": "mbytes"
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "IO Write MB"
            },
            "properties": [
              {
                "id": "unit",
                "value": "mbytes"
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "IO Stall (ms)"
            },
            "properties": [
              {
                "id": "unit",
                "value": "ms"
              }
            ]
          }
        ]
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm",
        "footer": {
          "show": false,
          "reducer": [
            "sum"
          ],
          "fields": ""
        },
        "sortBy": [
          {
            "displayName": "CPU Time (ms)",
            "desc": true
          }
        ]
      },
      "targets": [
        {
          "refId": "A",
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "darling"
          },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "\nWITH u AS (SELECT * FROM (\nWITH workload AS (\n    SELECT server_id, database_name,\n           SUM(worker_time_sum) / 1000.0 AS cpu_time_ms,\n           SUM(logical_reads_sum) AS logical_reads,\n           SUM(physical_reads_sum) AS physical_reads,\n           SUM(logical_writes_sum) AS logical_writes,\n           SUM(execution_count_sum) AS execution_count\n    FROM collect.query_stats_db_daily\n    WHERE server_id IN (${server:csv}) AND $__timeFilter(bucket)\n    GROUP BY server_id, database_name\n),\nio AS (\n    SELECT server_id, database_name,\n           SUM(delta_read_bytes) / 1048576.0 AS io_read_mb,\n           SUM(delta_write_bytes) / 1048576.0 AS io_write_mb,\n           SUM(delta_stall_read_ms + delta_stall_write_ms) AS io_stall_ms\n    FROM collect.v_file_io_stats\n    WHERE server_id IN (${server:csv})\n      AND $__timeFilter(collection_time)\n      AND delta_read_bytes IS NOT NULL\n    GROUP BY server_id, database_name\n),\ncombined AS (\n    SELECT COALESCE(w.server_id, i.server_id) AS server_id,\n           COALESCE(w.database_name, i.database_name) AS database_name,\n           COALESCE(w.cpu_time_ms, 0) AS cpu_time_ms,\n           COALESCE(w.logical_reads, 0) AS logical_reads,\n           COALESCE(w.physical_reads, 0) AS physical_reads,\n           COALESCE(w.logical_writes, 0) AS logical_writes,\n           COALESCE(w.execution_count, 0) AS execution_count,\n           COALESCE(i.io_read_mb, 0) AS io_read_mb,\n           COALESCE(i.io_write_mb, 0) AS io_write_mb,\n           COALESCE(i.io_stall_ms, 0) AS io_stall_ms\n    FROM workload w\n    FULL JOIN io i ON i.server_id = w.server_id AND i.database_name = w.database_name\n),\ntotals AS (\n    SELECT server_id,\n           NULLIF(SUM(cpu_time_ms), 0) AS total_cpu,\n           NULLIF(SUM(io_read_mb + io_write_mb), 0) AS total_io\n    FROM combined\n    GROUP BY server_id\n)\nSELECT\n    c.server_id,\n    c.database_name,\n    c.cpu_time_ms,\n    c.logical_reads,\n    c.physical_reads,\n    c.logical_writes,\n    c.execution_count,\n    c.io_read_mb,\n    c.io_write_mb,\n    c.io_stall_ms,\n    c.cpu_time_ms * 100.0 / t.total_cpu AS pct_cpu_share,\n    (c.io_read_mb + c.io_write_mb) * 100.0 / t.total_io AS pct_io_share\nFROM combined c\nJOIN totals t ON t.server_id = c.server_id\nWHERE c.database_name IS NOT NULL\n) AS daily_tier WHERE (CASE WHEN $__timeFrom() >= (now() AT TIME ZONE 'UTC') - INTERVAL '3 days' THEN 'raw' WHEN $__timeFrom() < (now() AT TIME ZONE 'UTC') - INTERVAL '3 days' AND $__timeFrom() >= (now() AT TIME ZONE 'UTC') - INTERVAL '20 days' THEN CASE WHEN (SELECT min(bucket) FROM collect.query_stats_db_hourly) <= $__timeFrom() THEN 'hourly' WHEN (SELECT min(collection_time) FROM collect.v_query_stats) < COALESCE((SELECT min(bucket) FROM collect.query_stats_db_hourly), 'infinity'::timestamp) THEN 'raw' ELSE 'hourly' END WHEN $__timeFrom() < (now() AT TIME ZONE 'UTC') - INTERVAL '20 days' THEN CASE WHEN (SELECT min(bucket) FROM collect.query_stats_db_daily) <= $__timeFrom() THEN 'daily' WHEN (SELECT min(bucket) FROM collect.query_stats_db_hourly) < COALESCE((SELECT min(bucket) FROM collect.query_stats_db_daily), 'infinity'::timestamp) AND (SELECT min(bucket) FROM collect.query_stats_db_hourly) <= $__timeFrom() THEN 'hourly' WHEN (SELECT min(bucket) FROM collect.query_stats_db_hourly) < COALESCE((SELECT min(bucket) FROM collect.query_stats_db_daily), 'infinity'::timestamp) AND (SELECT min(collection_time) FROM collect.v_query_stats) < COALESCE((SELECT min(bucket) FROM collect.query_stats_db_hourly), 'infinity'::timestamp) THEN 'raw' WHEN (SELECT min(bucket) FROM collect.query_stats_db_hourly) < COALESCE((SELECT min(bucket) FROM collect.query_stats_db_daily), 'infinity'::timestamp) THEN 'hourly' WHEN (SELECT min(collection_time) FROM collect.v_query_stats) < COALESCE((SELECT min(bucket) FROM collect.query_stats_db_daily), 'infinity'::timestamp) THEN 'raw' ELSE 'daily' END END) = 'daily'\nUNION ALL\nSELECT * FROM (\nWITH workload AS (\n    SELECT server_id, database_name,\n           SUM(worker_time_sum) / 1000.0 AS cpu_time_ms,\n           SUM(logical_reads_sum) AS logical_reads,\n           SUM(physical_reads_sum) AS physical_reads,\n           SUM(logical_writes_sum) AS logical_writes,\n           SUM(execution_count_sum) AS execution_count\n    FROM collect.query_stats_db_hourly\n    WHERE server_id IN (${server:csv}) AND $__timeFilter(bucket)\n    GROUP BY server_id, database_name\n),\nio AS (\n    SELECT server_id, database_name,\n           SUM(delta_read_bytes) / 1048576.0 AS io_read_mb,\n           SUM(delta_write_bytes) / 1048576.0 AS io_write_mb,\n           SUM(delta_stall_read_ms + delta_stall_write_ms) AS io_stall_ms\n    FROM collect.v_file_io_stats\n    WHERE server_id IN (${server:csv})\n      AND $__timeFilter(collection_time)\n      AND delta_read_bytes IS NOT NULL\n    GROUP BY server_id, database_name\n),\ncombined AS (\n    SELECT COALESCE(w.server_id, i.server_id) AS server_id,\n           COALESCE(w.database_name, i.database_name) AS database_name,\n           COALESCE(w.cpu_time_ms, 0) AS cpu_time_ms,\n           COALESCE(w.logical_reads, 0) AS logical_reads,\n           COALESCE(w.physical_reads, 0) AS physical_reads,\n           COALESCE(w.logical_writes, 0) AS logical_writes,\n           COALESCE(w.execution_count, 0) AS execution_count,\n           COALESCE(i.io_read_mb, 0) AS io_read_mb,\n           COALESCE(i.io_write_mb, 0) AS io_write_mb,\n           COALESCE(i.io_stall_ms, 0) AS io_stall_ms\n    FROM workload w\n    FULL JOIN io i ON i.server_id = w.server_id AND i.database_name = w.database_name\n),\ntotals AS (\n    SELECT server_id,\n           NULLIF(SUM(cpu_time_ms), 0) AS total_cpu,\n           NULLIF(SUM(io_read_mb + io_write_mb), 0) AS total_io\n    FROM combined\n    GROUP BY server_id\n)\nSELECT\n    c.server_id,\n    c.database_name,\n    c.cpu_time_ms,\n    c.logical_reads,\n    c.physical_reads,\n    c.logical_writes,\n    c.execution_count,\n    c.io_read_mb,\n    c.io_write_mb,\n    c.io_stall_ms,\n    c.cpu_time_ms * 100.0 / t.total_cpu AS pct_cpu_share,\n    (c.io_read_mb + c.io_write_mb) * 100.0 / t.total_io AS pct_io_share\nFROM combined c\nJOIN totals t ON t.server_id = c.server_id\nWHERE c.database_name IS NOT NULL\n) AS hourly_tier WHERE (CASE WHEN $__timeFrom() >= (now() AT TIME ZONE 'UTC') - INTERVAL '3 days' THEN 'raw' WHEN $__timeFrom() < (now() AT TIME ZONE 'UTC') - INTERVAL '3 days' AND $__timeFrom() >= (now() AT TIME ZONE 'UTC') - INTERVAL '20 days' THEN CASE WHEN (SELECT min(bucket) FROM collect.query_stats_db_hourly) <= $__timeFrom() THEN 'hourly' WHEN (SELECT min(collection_time) FROM collect.v_query_stats) < COALESCE((SELECT min(bucket) FROM collect.query_stats_db_hourly), 'infinity'::timestamp) THEN 'raw' ELSE 'hourly' END WHEN $__timeFrom() < (now() AT TIME ZONE 'UTC') - INTERVAL '20 days' THEN CASE WHEN (SELECT min(bucket) FROM collect.query_stats_db_daily) <= $__timeFrom() THEN 'daily' WHEN (SELECT min(bucket) FROM collect.query_stats_db_hourly) < COALESCE((SELECT min(bucket) FROM collect.query_stats_db_daily), 'infinity'::timestamp) AND (SELECT min(bucket) FROM collect.query_stats_db_hourly) <= $__timeFrom() THEN 'hourly' WHEN (SELECT min(bucket) FROM collect.query_stats_db_hourly) < COALESCE((SELECT min(bucket) FROM collect.query_stats_db_daily), 'infinity'::timestamp) AND (SELECT min(collection_time) FROM collect.v_query_stats) < COALESCE((SELECT min(bucket) FROM collect.query_stats_db_hourly), 'infinity'::timestamp) THEN 'raw' WHEN (SELECT min(bucket) FROM collect.query_stats_db_hourly) < COALESCE((SELECT min(bucket) FROM collect.query_stats_db_daily), 'infinity'::timestamp) THEN 'hourly' WHEN (SELECT min(collection_time) FROM collect.v_query_stats) < COALESCE((SELECT min(bucket) FROM collect.query_stats_db_daily), 'infinity'::timestamp) THEN 'raw' ELSE 'daily' END END) = 'hourly'\nUNION ALL\nSELECT * FROM (\nWITH workload AS (\n    SELECT server_id, database_name,\n           SUM(delta_worker_time) / 1000.0 AS cpu_time_ms,\n           SUM(delta_logical_reads) AS logical_reads,\n           SUM(delta_physical_reads) AS physical_reads,\n           SUM(delta_logical_writes) AS logical_writes,\n           SUM(delta_execution_count) AS execution_count\n    FROM collect.v_query_stats\n    WHERE server_id IN (${server:csv})\n      AND $__timeFilter(collection_time)\n      AND delta_worker_time IS NOT NULL\n    GROUP BY server_id, database_name\n),\nio AS (\n    SELECT server_id, database_name,\n           SUM(delta_read_bytes) / 1048576.0 AS io_read_mb,\n           SUM(delta_write_bytes) / 1048576.0 AS io_write_mb,\n           SUM(delta_stall_read_ms + delta_stall_write_ms) AS io_stall_ms\n    FROM collect.v_file_io_stats\n    WHERE server_id IN (${server:csv})\n      AND $__timeFilter(collection_time)\n      AND delta_read_bytes IS NOT NULL\n    GROUP BY server_id, database_name\n),\ncombined AS (\n    SELECT COALESCE(w.server_id, i.server_id) AS server_id,\n           COALESCE(w.database_name, i.database_name) AS database_name,\n           COALESCE(w.cpu_time_ms, 0) AS cpu_time_ms,\n           COALESCE(w.logical_reads, 0) AS logical_reads,\n           COALESCE(w.physical_reads, 0) AS physical_reads,\n           COALESCE(w.logical_writes, 0) AS logical_writes,\n           COALESCE(w.execution_count, 0) AS execution_count,\n           COALESCE(i.io_read_mb, 0) AS io_read_mb,\n           COALESCE(i.io_write_mb, 0) AS io_write_mb,\n           COALESCE(i.io_stall_ms, 0) AS io_stall_ms\n    FROM workload w\n    FULL JOIN io i ON i.server_id = w.server_id AND i.database_name = w.database_name\n),\ntotals AS (\n    SELECT server_id,\n           NULLIF(SUM(cpu_time_ms), 0) AS total_cpu,\n           NULLIF(SUM(io_read_mb + io_write_mb), 0) AS total_io\n    FROM combined\n    GROUP BY server_id\n)\nSELECT\n    c.server_id,\n    c.database_name,\n    c.cpu_time_ms,\n    c.logical_reads,\n    c.physical_reads,\n    c.logical_writes,\n    c.execution_count,\n    c.io_read_mb,\n    c.io_write_mb,\n    c.io_stall_ms,\n    c.cpu_time_ms * 100.0 / t.total_cpu AS pct_cpu_share,\n    (c.io_read_mb + c.io_write_mb) * 100.0 / t.total_io AS pct_io_share\nFROM combined c\nJOIN totals t ON t.server_id = c.server_id\nWHERE c.database_name IS NOT NULL\n) AS raw_tier WHERE (CASE WHEN $__timeFrom() >= (now() AT TIME ZONE 'UTC') - INTERVAL '3 days' THEN 'raw' WHEN $__timeFrom() < (now() AT TIME ZONE 'UTC') - INTERVAL '3 days' AND $__timeFrom() >= (now() AT TIME ZONE 'UTC') - INTERVAL '20 days' THEN CASE WHEN (SELECT min(bucket) FROM collect.query_stats_db_hourly) <= $__timeFrom() THEN 'hourly' WHEN (SELECT min(collection_time) FROM collect.v_query_stats) < COALESCE((SELECT min(bucket) FROM collect.query_stats_db_hourly), 'infinity'::timestamp) THEN 'raw' ELSE 'hourly' END WHEN $__timeFrom() < (now() AT TIME ZONE 'UTC') - INTERVAL '20 days' THEN CASE WHEN (SELECT min(bucket) FROM collect.query_stats_db_daily) <= $__timeFrom() THEN 'daily' WHEN (SELECT min(bucket) FROM collect.query_stats_db_hourly) < COALESCE((SELECT min(bucket) FROM collect.query_stats_db_daily), 'infinity'::timestamp) AND (SELECT min(bucket) FROM collect.query_stats_db_hourly) <= $__timeFrom() THEN 'hourly' WHEN (SELECT min(bucket) FROM collect.query_stats_db_hourly) < COALESCE((SELECT min(bucket) FROM collect.query_stats_db_daily), 'infinity'::timestamp) AND (SELECT min(collection_time) FROM collect.v_query_stats) < COALESCE((SELECT min(bucket) FROM collect.query_stats_db_hourly), 'infinity'::timestamp) THEN 'raw' WHEN (SELECT min(bucket) FROM collect.query_stats_db_hourly) < COALESCE((SELECT min(bucket) FROM collect.query_stats_db_daily), 'infinity'::timestamp) THEN 'hourly' WHEN (SELECT min(collection_time) FROM collect.v_query_stats) < COALESCE((SELECT min(bucket) FROM collect.query_stats_db_daily), 'infinity'::timestamp) THEN 'raw' ELSE 'daily' END END) = 'raw')\nSELECT\n    srv.name AS \"Server\",\n    u.database_name AS \"Database\",\n    round(u.cpu_time_ms, 0) AS \"CPU Time (ms)\",\n    round(u.pct_cpu_share, 1) AS \"CPU %\",\n    u.logical_reads AS \"Logical Reads\",\n    u.physical_reads AS \"Physical Reads\",\n    u.logical_writes AS \"Logical Writes\",\n    u.execution_count AS \"Executions\",\n    round(u.io_read_mb, 2) AS \"IO Read MB\",\n    round(u.io_write_mb, 2) AS \"IO Write MB\",\n    round(u.pct_io_share, 1) AS \"IO %\",\n    u.io_stall_ms AS \"IO Stall (ms)\"\nFROM u\nJOIN config.config_monitored_servers AS srv ON srv.server_id = u.server_id\nORDER BY u.cpu_time_ms DESC\n"
        }
      ],
      "description": "Over the dashboard range. CPU % and IO % are shares of that server's own total."
    }
  ]
}
